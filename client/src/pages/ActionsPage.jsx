import AppShell from '../components/layout/AppShell'

const ActionsPage = () => {
  return (
    <AppShell activeTab="detailed">
      <section className="card">
        <h2 className="section-title">Actions (kommer snart)</h2>
        <p>Her vil du få en oversikt over alle anbefalte tiltak.</p>
      </section>
    </AppShell>
  )
}

export default ActionsPage
